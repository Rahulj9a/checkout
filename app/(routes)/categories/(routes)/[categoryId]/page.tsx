
interface CategoryPageProps{
params:{
    categoryId:string
}
}

const CategoryPage:React.FC<CategoryPageProps> = async({params}) => {
   /*  const categoryg = await getCate(params.categoryId) */
    return (<div>

    </div>)
}
export default CategoryPage